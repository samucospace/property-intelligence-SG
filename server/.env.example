# Server Configuration
PORT=3001
NODE_ENV=production

# Security & Ingestion Access
# Protects /api/ingest/* endpoints from unauthorized public requests
ADMIN_API_KEY=change_this_to_a_secure_random_key_in_production

# URA Data Service Credentials (Used only on server by batch cron jobs)
URA_ACCESS_KEY=

# Automated Weekly Newsletter (Resend API)
RESEND_API_KEY=
NEWSLETTER_FROM_EMAIL=Property Intelligence SG <digest@yourdomain.sg>
BASE_URL=https://yourdomain.com

# Allowed Origins (Optional, default allows all for same-origin production)
# ALLOWED_ORIGIN=https://yourdomain.com
